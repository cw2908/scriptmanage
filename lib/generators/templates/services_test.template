require 'minitest/autorun'

describe Pservices::<%= name.singularize.camelcase %> do
  it 'is a loaded module' do
    assert_equal(Pservices::<%= name.singularize.camelcase %>.class, Module)
  end
end